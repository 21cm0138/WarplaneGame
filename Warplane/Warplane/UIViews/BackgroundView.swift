//
//  BackgroundView.swift
//  Warplane
//
//  Created by cmStudent on 2022/07/29.
//

import SwiftUI

struct BackgroundView: View {
    
    private let screenWidth = UIScreen.main.bounds.width
    let screenHeight = UIScreen.main.bounds.height
    
    var blockSpace:CGFloat = 14.0
    
    
    
    var body: some View {
        
        ZStack{
            VStack{
                
                Group{
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                }
                Group{
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                }
                Group{
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                    VStack{}
                        .frame(width: screenWidth, height: 1, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.vertical, blockSpace)
                }
                
                
                
                
            }.frame(width: screenWidth, height: screenHeight, alignment: .center)
            
            HStack{
                Group{
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                }
                Group{
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                }
                Group{
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                    VStack{}
                        .frame(width: 1, height: screenHeight, alignment: .center)
                        .background(Color("Color_line"))
                        .padding(.horizontal, blockSpace)
                }
                
            }.frame(width: screenWidth, height: screenHeight, alignment: .center)
            
            ZStack{
                VStack{}
                    .frame(width: screenWidth, height: 1, alignment: .center)
                    .background(Color.red)
                VStack{}
                    .frame(width: 1, height: screenHeight, alignment: .center)
                    .background(Color.red)
                
                HStack{
                    VStack{}
                        .frame(width: 1, height: 210, alignment: .center)
                        .background(Color.green)
                    
                    VStack{
                        VStack{}
                            .frame(width: 210, height: 1, alignment: .center)
                            .background(Color.green)
                            .padding(.bottom, 105.0)
                        
                        VStack{}
                            .frame(width: 210, height: 1, alignment: .center)
                            .background(Color.green)
                            .padding(.top, 105.0)
                        
                    }.padding(.horizontal, -4.0)
                    
                    VStack{}
                        .frame(width: 1, height: 210, alignment: .center)
                        .background(Color.green)
                    
                }
                HStack{
                    VStack{}
                        .frame(width: 1, height: 65, alignment: .center)
                        .background(Color.red)
                    
                    
                    
                    VStack{
                        VStack{}
                            .frame(width: 65, height: 1, alignment: .center)
                            .background(Color.red)
                            .padding(.bottom, 30.0)
                        VStack{}
                            .frame(width: 65, height: 1, alignment: .center)
                            .background(Color.red)
                            .padding(.top, 30.0)
                    }.padding(.horizontal, -7.0)
                    
                    VStack{}
                        .frame(width: 1, height: 65, alignment: .center)
                        .background(Color.red)
                        .padding(.all, 0.0)
                    
                    
                }
                
                
            }
            
        }.frame(width: screenWidth, height: screenHeight, alignment: .center)
            .background(Color("Color_background"))
    }
}

struct BackgroundView_Previews: PreviewProvider {
    static var previews: some View {
        BackgroundView()
    }
}


